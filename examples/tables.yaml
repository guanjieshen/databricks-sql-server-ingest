connection:
  server: your-server.database.windows.net
  sql_login: sqladmin
  password: ${ADMIN_PASSWORD}

storage:
  data_dir: ./data
  watermark_dir: ./watermarks

databases:
  database_1:
    dbo:
      orders: full_incremental
      customers: full_incremental
      products: full_incremental
      inventory: full
      audit_log: incremental
